import './pages/index.css';
import Card from './components/Card';

// eslint-disable-next-line no-undef
const { createApp } = Vue;

createApp({
  data() {
    return {
    };
  },
  components: {
    Card,
  },
}).mount('#app');
